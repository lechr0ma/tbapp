import { StyleSheet } from "react-native"

const  layoyt = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 20,
        height: "100%"
    },
    button:{ backgroundColor: "white", width: 100, height: 100, alignItems: "center", justifyContent: "center" },
    link: {
        marginTop: 15,
        paddingVertical: 15,
    },
})

export default  layoyt